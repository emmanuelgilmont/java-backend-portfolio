services:
  elasticsearch-formation-search:
    image: elasticsearch-formation-search:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_ELASTICSEARCH_URIS=http://192.168.2.30:9200
    restart: unless-stopped