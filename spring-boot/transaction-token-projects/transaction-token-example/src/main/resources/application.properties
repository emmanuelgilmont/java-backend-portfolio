spring.application.name=transaction-token-example
server.port=8080

# --- Token Context Starter configuration ---
# Uncomment to disable the built-in GlobalExceptionHandler
# (e.g. if you provide your own @RestControllerAdvice)
# token-context.exception-handler.enabled=false
